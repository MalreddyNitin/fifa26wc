# dbt documentation

Install the warehouse extra, start PostgreSQL, load the canonical Parquet
tables, then run `dbt docs generate --project-dir dbt --profiles-dir dbt`.
Generated catalog files are intentionally not committed.
